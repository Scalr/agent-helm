{{- if not .Values.tokenExistingSecret.name }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent-local.name" . }}-token
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "agent-local.labels" . | nindent 4 }}
    {{- with .Values.secret.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.secret.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
data:
  token: {{ required ".Values.token must be provided or a pre-existing secret must be specified in .Values.tokenExistingSecret!" .Values.token | b64enc | quote }}
{{- end }}
